# 🧩 Componentes

## <LoginPage />

Renderiza a tela de login adaptada automaticamente com base nos providers fornecidos.

- Se houver `"credentials"` → renderiza campos de email/senha
- Se houver OAuth (ex: `google`, `discord`) → renderiza botões de login social

## <RegisterPage />

Idêntico ao LoginPage, porém com campos de cadastro.

- Usa `useRegisterForm`
- Responsável por enviar dados via `fetch` para `/auth/register`