# 🧱 Layouts visuais

Você pode definir o layout visual das telas com:

```ts
ui: {
  variant: "split-left" | "split-right" | "minimal" | "background-image"
}
```

Cada layout pode ter seu próprio estilo de marca, imagem e estrutura.

Você também pode clonar e criar seu próprio layout no repositório.