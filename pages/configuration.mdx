# ⚙️ Configuração

```tsx
<AuthProvider
  config={{
    baseUrl: "https://api.meusistema.com",
    providers: ["credentials", "google"],
    theme: {
      primaryColor: "#716C4A",
      fontFamily: "Yeseva One"
    }
  }}
>
```