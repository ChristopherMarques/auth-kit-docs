# 🚀 Começando

## Instalação

```bash
pnpm add @sawabona/auth-kit
```

## Pré-requisitos

- Next.js 13+ com App Router
- React 18+
- TailwindCSS + ShadCN
- NextAuth configurado no backend

## Setup básico

```tsx
import { AuthProvider } from "@sawabona/auth-kit";

<AuthProvider config={{
  baseUrl: "https://api.suaapi.com",
  providers: ["credentials"]
}}>
  <App />
</AuthProvider>
```